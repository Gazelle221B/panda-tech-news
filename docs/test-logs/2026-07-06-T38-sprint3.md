# T38〜T41 — Sprint 3 配信 (波形動画 + YouTube 限定公開 + 朝確認フロー v0.5)

- 日付: 2026-07-06 (実装) / 2026-07-12 (main 追随・ADR-0008 適合)
- ブランチ: `agent/T38-sprint3-impl` ([PR #25](https://github.com/Gazelle221B/panda-tech-news/pull/25))
- 実装者: Claude Code
- 参照: [IMPLEMENTATION_PLAN-3.md](../IMPLEMENTATION_PLAN-3.md), [ADR-0007](../adr/ADR-0007-youtube-httpx-cli-approval.md)

> 本ファイルは、実装当時 `docs/PROJECT_STATE.md` に直接追記していた Sprint 3 の進捗・証跡を、
> ADR-0008 (2026-07-11 発効、impl ブランチでの PROJECT_STATE.md 編集禁止) に適合させるため
> チケットログとして移設したもの。PROJECT_STATE.md 本体の更新はマージ後の docs ブランチで行う。

## 実装内容

roadmap Sprint 3 (波形動画 / YouTube 限定公開 / AI 開示 / 朝確認フロー) の v0.5 受け入れ基準
「限定公開 YouTube 動画が生成・投稿できる」までの経路をコードで完成させた。

- **T38**: `video/render.py` — mp3 + ロゴ静止画 (無ければ単色縮退) + showwaves 波形 → 720p H.264/AAC mp4 (FR-110/111/112)。ffmpeg 単発呼び出し・タイムアウト必須・純ロジック分離。`-loop 1` は壊れた画像で ffmpeg がハングするため不採用 (実機で 900s ハングを観測)、lavfi color ベース + 単一フレーム overlay で fail-fast。
- **T39**: `deliver/youtube.py` — OAuth refresh token 方式 + resumable upload (FR-120/122)、httpx のみで新規依存ゼロ (ADR-0007)。説明欄への AI 開示挿入をコードで強制し `containsSyntheticMedia=true` も送る (FR-121 二重開示)。`youtube-auth` CLI (loopback / --manual) で初回 refresh token 取得。
- **T40**: `video_versions` テーブル + repo 4関数 + `publish` / `approve` CLI。publish は unlisted/private のみ受け付け (public 拒否、公開は人間の approve のみ)、アップロード失敗は fail-fast・Discord 通知は fail-open。朝確認メッセージ (✅ approve / 🔁 再生成 / ❌ 見送り) を Discord へ投稿。
- **T41**: `daily_pipeline.sh` に `PUBLISH_YOUTUBE=1` オプトインの publish 段を追加 (既定 off。produce 失敗日はスキップし古い音声を配信しない)。.env.example / README (セットアップ手順) / AGENTS / ADR INDEX を同期。

## 品質ゲート (実装時, 2026-07-06)

- `uv run pytest` **492 passed** (ベースライン 438 → +54。実 ffmpeg 統合テスト含む)
- `uv run ruff check .` clean / `uv run mypy src tests` strict clean (76 files)
- `shellcheck scripts/daily_pipeline.sh` clean / `uv lock --check` OK
- E2E 検証: 実 mp3 (8s) → 実 CLI `publish --dry-run` → 720p mp4 生成を確認
- Codex (gpt-5.4) 独立レビュー: High 1 / Medium 2 / Low 1 → 全件反映済み (videos.update の mutable whitelist、JSON 破損の正規化、DB 失敗時の復旧情報、ポート使用中の案内)

## main 追随 (2026-07-12)

- `origin/main` (T44〜T49 マージ後, `1122512`) を本ブランチへマージ。
- コンフリクト解消: `docs/PROJECT_STATE.md` は ADR-0008 に従い **main 版を全面採用** (ブランチ側の Sprint 3 追記は本ファイルへ移設)。`docs/adr/INDEX.md` は ADR-0007 行 + ADR-0008 行の union + 効き方に Sprint 3 行を追加。
- 追随後の fresh ゲート結果は本ファイル末尾「品質ゲート (main 追随後)」に記録。

## 残作業 (人間、マージ後)

1. GCP プロジェクト + YouTube Data API v3 + OAuth クライアント (デスクトップ) 作成 → README「YouTube 配信セットアップ」
2. `uv run karyu youtube-auth` で refresh token を取得し .env へ
3. 実アップロード smoke (`publish` → YouTube Studio 確認 → 必要なら `approve`)
4. ロゴ素材 `assets/logo.png` (任意。無くても単色背景で配信可)
